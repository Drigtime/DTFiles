# get-dofus-map-files

```shell
# Install ImageMagick
https://imagemagick.org/script/download.php

# Install all the dependency :
Yarn

# Fix image-map-tiles dependency
comment `.crop(sharp.gravity.center)` at line `257` in `node_modules/image-map-tiles/index.js`
```
